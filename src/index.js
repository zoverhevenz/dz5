import React from "react";
import ReactDOM from "react-dom/client";
import "./index.css";
import PrototypeFormComponent from "./PrototypeFormComponent";

const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(
  <React.StrictMode>
    <PrototypeFormComponent />
  </React.StrictMode>
);
